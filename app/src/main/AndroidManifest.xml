<?xml version="1.0" encoding="utf-8"?>
<manifest package="com.guilhermylg.calculadoraimc"
    xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:label="@string/app_name"
        android:icon="@android:drawable/sym_def_app_icon"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">

        <!-- Tela Principal -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Outras Activities -->
        <activity android:name=".CalculoIMCActivity" />
        <activity android:name=".AbaixoDoPesoActivity" />
        <activity android:name=".PesoNormalActivity" />
        <activity android:name=".SobrepesoActivity" />
        <activity android:name=".Obesidade1Activity" />
        <activity android:name=".Obesidade2Activity" />
        <activity android:name=".Obesidade3Activity" />

    </application>
</manifest>
